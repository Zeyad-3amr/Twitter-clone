import { styled } from 'styled-components';

export const UserTweetP = styled.p`
  color: ${({ theme }) => theme.theme.textColor};
  margin: 0;
  padding: 0.4rem;
  padding-left: 0rem;
  word-break: break-word;
`;
